spring:
  application:
    name: eureka-server

server:
  port: 8761

zipkin:
  base-url: http://localhost:9411

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

cloud:
  discovery:
    enabled: false
